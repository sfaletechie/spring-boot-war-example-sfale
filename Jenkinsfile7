pipeline {
  agent any
  tools {
    maven 'Maven'
  }
  stages {
    stage('Build') {
      steps {
        sh 'mvn package'
        sh 'jfrog rt upload --url http://192.168.29.22:8082/artifactory/ --access-token ${ARTIFACTORY_ACCESS_TOKEN} **/*.war java-web-app/'
      }
    }
  }
}
